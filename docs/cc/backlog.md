# Execution-integrity backlog — remaining work

Everything in the feature baseline and the post-baseline hardening set is **merged** to
`coco-parity` and documented in `docs/cc/parma-hardening-features.md` (the single source of
truth for shipped features). This file tracks only what is **not yet complete**: open work
items and the merged features whose *live* end-to-end validation is deployment-time.

_For the full list of shipped items and their commits/PRs, see
`docs/cc/parma-hardening-features.md`._

## Open work items

FR-16 (complete OCI Process field coverage in genpolicy — `workingDir`/apparmor/rlimits
exact-match plus an on-wire OCI field-coverage CI gate) is **delivered in this PR**; see
`parma-hardening-features.md` §"Complete OCI Process field coverage in genpolicy (FR-16)".
All earlier execution-integrity work items (BL-1…BL-9) are merged to `coco-parity`.
BL-5 (bind measured state into initdata) landed in PR #10 (branch `bl5-initdata-measured`) — see
`parma-hardening-features.md` §"Measured-initdata trust roots". Besides FR-16, what remains is **live
validation** of already-merged features (below).

### Documentation completeness (DOC-1…DOC-3)

`parma-hardening-features.md` documents each feature as **Gap → Fix → Guarantee → Commits →
Validated**, which explains what each feature does but not the reasoning the set as a whole
rests on. Three additions would make the document self-justifying to a reader outside the
team, and would make over-claims harder to introduce.

| ID    | Item                                                                                                                                                                                                                                                                        | Why                                                                                                                                                                                                                                                        |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DOC-1 | **Threat model section.** State the adversary (untrusted host and shim), the trust boundary, the assumptions inherited from the hardware TEE, and what is explicitly *not* defended (host-controlled scheduling and resource starvation, denial of service, side channels). | Every feature is currently justified against an unstated model, so "is this a gap?" has no written arbiter. This is the first thing an external reviewer needs.                                                                                            |
| DOC-2 | **Parity anchors.** For each feature, name the equivalent mechanism in the runhcs/OpenGCS confidential stack and state whether this branch matches it or deliberately differs, with the reason.                                                                             | The design is a parity effort, but the comparison appears nowhere in the docs and only once in the code (`policy.rs` citing `WithMetadataRollback`). Recording it turns "we chose differently" into a reviewable decision instead of an apparent omission. |
| DOC-3 | **A `Limits` bullet per feature**, stating what the guarantee does *not* cover.                                                                                                                                                                                             | A **Guarantee** with no matching **Limits** is how a requirement's text drifts ahead of its implementation. FR-6 now carries one; the rest do not.                                                                                                         |

### Reference-monitor correctness (RM-1…RM-12)

Open items in the FR-6 transaction machinery. RM-1 and RM-2 are the halves deliberately
left out of the transaction-lifecycle fix; RM-3…RM-5 are pre-existing.

| ID    | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Why                                                                                                                                                                                                                                                                                                                                               |
| ----- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RM-1  | **Serialize concurrent operations on the same id.** `prepare` now *refuses* a duplicate while one is in flight, but the SRM lock is still released before the runtime operation runs, so the refusal is a clean failure rather than serialization. runhcs takes a per-container `TryLock` for the duration.                                                                                                                                                                                                                                                     | Two concurrent requests for the same container id are host-triggerable. Failing cleanly is correct but returns an error for what may be a legitimate retry; holding the lock would let the second wait.                                                                                                                                           |
| RM-2  | ✅ **Closed — won't-fix as specified.** Replay protection for `SignalProcess` and `ExecProcess` is scoped to in-flight duplicates only, because their operation ids name repeatable events rather than unique objects, so their transactions retire on commit. The proposed fix — an idempotency key chosen by the initiator — is not deliverable against this threat model, and is not required for parity.                                                                 | Two independent reasons. (1) **No parity gap.** The runhcs/OpenGCS enforcer has no anti-replay on these operations at all: neither `exec_in_container` nor `signal_container_process` consults any "already done" state, so an identical exec or an identical `SIGTERM` is re-authorized indefinitely. Only container creation carries a state guard there, and this branch is stricter on that path. (2) **A key the initiator picks is a key the adversary picks.** The stated adversary is the untrusted host and shim (DOC-1), so a hostile initiator defeats an attempt/sequence number simply by choosing a fresh one. The scope argument previously recorded here — "needs a shim↔agent API change" — was also wrong: `ttrpc_context.metadata` already carries shim-populated OpenTelemetry context on every call (`tracer.rs`), so the channel exists. The blocker is trust, not plumbing. A real fix must pin the key to something the host cannot forge (policy-author- or tenant-signed), which is a different feature with a different trust root. |
| RM-3  | **Route `commit` failures into quarantine.** ✅ Delivered — all four call sites now act on the result, as does the fourth `abort` site (`signal_process`) which had been discarding its own.                                                                                                                                                                                                                                                                                                                                                                    | The abort path quarantined on unprovable state; the commit path did not. Asymmetric, and the commit path is the one that runs on success.                                                                                                                                                                                                         |
| RM-6  | ✅ **Test the SRM integration, not just the crate.** The four `rpc.rs` call sites had no test coverage at all — every reference-monitor test was crate-level, and the rpc test module never touched `SRM`. Added a `rpc::tests::srm_integration` module (10 tests) covering operation-id injectivity, `commit_or_quarantine` on both paths, `retire_or_warn`, the executed-binding op id, `abort_or_quarantine`, and the remove-frees-the-create-id sequence. **Partially closed, not closed:** these are in-process tests of the helpers, not of a live handler, and none of them cancels a future — see CI-8.                                                                                                                                                       | The crate is well tested and the defects keep being found in the wiring: F-19 (removal never wrapped), F-23 (replay cache misapplied), RM-3 (results discarded), and now F-24 (colliding operation ids), which this work found. Crate-level tests cannot see any of those.                                                                        |
| RM-7  | ✅ **Delivered.** Quarantine is now a distinct, machine-readable condition rather than an opaque `FAILED_PRECONDITION`: the monitor reports its health explicitly, so a caller can tell "this request was invalid" from "the guest can no longer prove anything" without inferring it from a second failure.                                                                                                                                                                                                                                                   | Quarantine is the fail-closed backstop for the whole of FR-6, and nothing propagated it. A host that cannot tell a rejected request from a dead enforcer will retry, and the retries look identical to an attack.                                                                                                                                |
| RM-8  | ✅ **Delivered.** Teardown is exempt. `prepare` no longer refuses removal and stop paths once quarantined, so a degraded guest can still be drained gracefully instead of leaving sandbox destruction as the only exit. The code comment claiming quarantine "leaves teardown paths usable" is true again.                                                                                                                                                                                                                                                      | Wrapping removal in a transaction was correct, but it changed quarantine's blast radius as a side effect and the comment silently became false. This is exactly the drift a per-feature `Limits` bullet (DOC-3) is meant to catch.                                                                                                              |
| RM-4  | ✅ **Injective operation ids (F-24).** Ids were built by concatenating host-controlled strings with unescaped separators: `{cid}`, `{cid}:{exec_id}`, `{cid}:{exec_id}:sig:{signal}`, `remove:{cid}`. A container named `a:b` produced the same id as an exec of `b` in container `a`; a container named `remove:foo` collided with the removal of `foo`. Fixed with a length-prefixed `srm_op_id(kind, parts)` builder applied at all four sites.                                                                                                              | Every collision consequence is a state-machine confusion in the component whose job is proving authorized == executed. Because a committed transaction is retained as a replay cache, a collision makes the agent return success for an operation it never performed — reachable by a host that merely chooses its own container and exec ids.    |
| RM-5  | ✅ **Delivered.** `formal/SRM.tla` was rewritten: it now models commit failure, binds `executed_digest` on `Execute` and checks it at `Commit`, guards `Quarantine`, and models future abandonment. The authorized-implies-executed property is now stated over the digest rather than over the state name alone.                                                                                                                                                                                                                                                | The model is what caught the `prepare` clobber (`Prepare(o)` requires `state[o] \in {"none","aborted"}`). Extending it was cheaper than finding the next divergence by inspection — and it did find more: the rewrite is what pinned the abandonment behaviour recorded in RM-10/RM-11.                                                             |
| RM-9  | **Two requests mutating the same policy key still race.** Rollback now reverts only the delta a failed request introduced, so a failed `remove(A)` no longer erases a concurrent `create(B)`. But the `before`/`after` bracket spans three separate `AGENT_POLICY` acquisitions, so any request that commits inside that window can be clobbered, not only one touching the *same* `pstate` key: a concurrent create landing between the failing request's `after` capture and its revert is read into `current` and then overwritten by the revert's whole-document rewrite. The proper fix is to hold authorization and its rollback under one lock, or to make `is_allowed` transactional in the enforcer itself.                                                 | Narrower than the whole-blob clobber it replaces — `pstate` is keyed by container id, and the two mutating rules (create, remove) are single-shot per container — but it is not closed. Worth stating explicitly so the delta rollback is not mistaken for full serialization.                                                                    |
| RM-10 | **An abandoned executed transaction quarantines, and the host chooses when.** ttrpc drops a handler future once the client-supplied `timeout_nano` elapses. If that happens between `execute` and `commit` the outcome is genuinely unknowable, so reclamation quarantines. A host can therefore force quarantine at will by sending a short timeout on any transactional RPC. This is deliberate — fail-closed beats an unkillable container — but it is a host-triggerable denial of service and pairs with RM-7/RM-8, which decide what recovery looks like. | The alternative (leave the id in flight) is worse: it is the permanently unremovable container this work exists to prevent. Recording it so the trade-off is a decision rather than an omission, and so quarantine recovery is designed knowing the host can trigger it.                                                                          |
| RM-11 | **Abandonment reclaims the SRM transaction but never the policy mirror.** `TxnGuard` records a dropped future so the next `prepare`/`execute` can abort or quarantine the SRM-side transaction, but nothing reverts the `pstate` delta that authorization already applied. A `create` cancelled after `is_allowed` leaves a phantom container in the enforcer; a `remove` cancelled after `is_allowed` leaves a real container the enforcer no longer knows about — the exact divergence FR-6 exists to prevent, reached by cancellation instead of by failure. | The reclamation path is the one exit that cannot call `rollback_policy_state`: a dropped future has no `.await` left to run. Closing it needs the policy delta stored *in* the transaction so the reclaimer can revert it, which is a design change rather than a patch. Host-triggerable via `timeout_nano`. |
| RM-12 | **`is_allowed` and `prepare` are not one atomic step.** Authorization mutates `pstate` and returns; the handler then acquires the SRM lock and calls `prepare`. Between those two points the request holds a committed policy mutation with no transaction covering it. Every early return in that window now reverts the delta, and cancellation in that window is RM-11, but the window itself remains. | The structural fix is to perform authorization and `prepare` under one lock, or to move the transaction boundary inside the enforcer. Recorded so the current mitigation is understood as a set of patched exits rather than as atomicity. |
| RM-13 | **Pin the container root path instead of merely allowing it to be rewritten.** The FR-3 plan binding classifies `/root/path` as *replaceable*, because `setup_bundle` legitimately rewrites the host-supplied value to the in-guest bundle rootfs and the authorized value cannot survive verbatim. The confidential runhcs/OpenGCS stack instead computes the expected rootfs path itself and rejects any spec whose `Root.Path` differs, so a container can only ever be rooted at the filesystem the platform prepared. The rewrite here is deterministic (`CONTAINER_BASE/<cid>/rootfs`), so the value can be pinned rather than waived. | Accepting *any* rewrite means a compromised transform between authorization and `create` could re-root the container at a directory the policy never saw, without tripping the binding. Bounded today by the fact that the rewriting code is itself inside the measured guest image, so this is a defence-in-depth divergence rather than a reachable bypass — but it is a divergence from the parity anchor, and closing it is a constant. |
| RM-14 | **Test the plan binding through the handler, not only as a library.** `plan_binding.rs` carries 23 unit tests, but each one calls `assert_within_resolution_bounds` directly with two hand-built specs. Nothing asserts that `create_container` captures the authorized spec *before* the first transform, reaches the check after `setup_bundle`, and surfaces a violation as a failed RPC rather than a log line. Needs an rpc-level test that drives the handler with a transform stubbed to mutate a pinned field, and asserts denial. | This is the same shape as the defect the binding replaces: a correct library property that the integration is assumed, but not shown, to use. A regression moving the clone after `add_devices`, or restoring the audit-only `info!`, would leave all 23 tests green. Same lesson as RM-6 — the crate is well tested and the defects keep being found in the wiring. |
| RM-15 | ✅ **Reverse the occurrence registry on a failed start.** `StartContainer` advanced the occurrence to `running` before the runtime start, and left it there if the start failed. The occurrence machine is what gates exec and signal, so a container that never started was nonetheless exec-able. Fixed by unwinding the transition on the failure arm.                                                                                                                                                                                        | Same class as RM-11: a state store mutated ahead of the operation it describes, with no path back. The occurrence machine is the *only* thing standing between a failed start and an exec into a container the runtime never ran, so the divergence was directly reachable rather than defence-in-depth.                                          |
| RM-16 | ✅ **Quarantine on an unprovable device binding.** A failure to record or retire a device binding was logged and swallowed, so the guest continued serving requests while its own record of which devices are attached had known-missing entries. Routed into quarantine, matching the treatment `commit` failures already received (RM-3).                                                                                                                                                                                                     | The asymmetry RM-3 fixed on the transaction path existed again on the binding path. An enforcer that cannot enumerate its own attached devices cannot prove any subsequent authorization about them, which is the definition of the quarantine condition.                                                                                        |
| RM-17 | ✅ **Gate `StartContainer` and the executed-binding attach on complete mediation.** Both reached mutating work on paths that the mediation manifest did not classify, so neither appeared in the FR-7 total-mediation test's view of the RPC surface. Both are now manifest-classified and gated, and the ordering was corrected so the network phase freezes only on the success arm.                                                                                                                                                            | FR-7's guarantee is that *every* RPC is classified — an unclassified mutating path is a hole in the guarantee itself, not merely an unenforced operation. Found by inverse-coverage sweep of the manifest against the handler table rather than by reading either one alone.                                                                     |
| RM-18 | **Two occurrence guards are implemented but not armed.** The registry supports a per-declaration cardinality bound and a monotonic per-alias generation; both are unit-tested and neither is reachable from the agent — `create` is called with `(None, None)`, and `assert_generation` has no caller. The requirement text in `parma-hardening-features.md` claimed both as delivered guarantees and has been corrected to record them as forward-looking capabilities. Arming either needs an RPC that carries an occurrence handle across a call. | Third instance of one pattern: a guard that exists, is unit-tested, and is claimed in the requirement text, but that no caller can reach (see also the tautological `expected_state_version` check, and the same defect in the cardinality bound). Not a reachable bypass — the host presents only an alias and every gate resolves it to the live occurrence — but a claimed property that no test can fail is how the text drifts ahead of the code. Either arm them or keep the text honest; the text is now honest. |
| RM-19 | ✅ **Retire the occurrence when the container's init process exits.** `OccurrenceRegistry::stop()` had no caller, so `Lifecycle::Stopped` was unreachable and an occurrence was only ever retired by `RemoveContainer` — a host RPC. A host that simply never called it held a dead container's occurrence in `running` indefinitely, and exec into it was admitted by the enforcer. Fixed by calling `stop()` from the agent's own SIGCHLD reaper (`signal.rs::handle_sigchild`), which is driven by the guest kernel rather than by the host, so the recorded lifecycle now follows the container. | The gate had to be split to do this safely: **exec** requires `Running`, **signal** requires `Running` or `Stopped`. The shim signals containers it has already reaped while tearing a pod down, so refusing signals to a stopped occurrence reproduces the F-75 failure mode — every pod on the node stuck `Terminating`. FR-9's guarantee text has been narrowed accordingly: it is exec, not signal, that is closed on a not-running occurrence. This now goes beyond the baseline, which records `containerTerminated` but gates neither. |
| RM-20 | ✅ **A container id is consumed for the sandbox's lifetime.** `create` rejected a duplicate alias only while the existing occurrence was not `Removed`, and `RemoveContainerRequest` called `state_del_key`, freeing the rego binding too — so the same host-chosen id could name a second occurrence. Fixed at both layers: the enforcer returns the new `AliasRetired` error for any create against a removed alias (a one-way latch, matching hcsshim, where `data.metadata.started[containerID]` is never cleared — not even by `shutdown_container`), and `RemoveContainerRequest` now records a `retired:<id>` tombstone that `CreateContainerRequest` refuses. | The blocking question is answered: CRI container ids are unique per container *instance* — a restart-policy restart gets a fresh id — so no legitimate flow recreates an id inside one sandbox. Deleting the id's own state key is still right, and still what makes every later start/exec/signal/remove undefined; the tombstone is a separate key precisely so it cannot be mistaken for a live binding. What is recovered is the structural property that an id names at most one occurrence, which is what makes "the host-supplied id is an untrusted alias" harmless in the baseline. |
| RM-21 | ✅ **A settings key an older `genpolicy` does not know is now a generation-time error, not a silent drop.** `RequestDefaults` is deserialized and then *re-serialized* into the generated policy, so any unknown key vanished without a warning. `SignalProcessRequest.allowed_signals` was such a key: with it missing the rule is undefined, every signal is denied fail-closed, kubelet can never kill the container, and every pod started that way is stuck `Terminating` with a leaked sandbox. Reproduced end to end on the dev node with the nightly binary (unkillable) and the branch binary (deleted in ~1s). Fixed with `#[serde(deny_unknown_fields)]` on `RequestDefaults`. | The rego is right to fail closed; the defect was a supply-chain skew between three artefacts that ship separately — `rules.rego`, `genpolicy-settings.json`, and the `genpolicy` binary — with nothing pinning them together. A policy whose security depends on a data key it cannot verify is present is one release-ordering mistake away from either an outage (this case) or, for a key whose absence *widens* a rule, a silent weakening. The check is deliberately loud and breaking: a settings file from a newer tree is now rejected by an older binary instead of quietly generating a policy that does not say what it appears to say. Verified that both the in-repo and installed settings files still parse, including the deprecated `ExecProcessRequest.commands` key. |

### Testing and CI

| ID   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CI-0 | ✅ **There is no CI. GitHub Actions is disabled on this fork** — `gh api .../actions/workflows` reports `total_count: 0` and `gh run list` is empty, so not one workflow has ever run and `gh pr checks` reports "no checks reported" on every PR. The workflow YAML in `.github/workflows` is inherited from upstream and is inert. Enabling Actions is a prerequisite for CI-1 through CI-4.                                                                                                                | Every "CI is green" inference about this work was vacuous — nothing has ever been built, linted, or tested automatically. This was found only by asking why a PR had no checks, and it immediately produced CI-5: the tree does not pass its own `make check`. Until Actions is on, each item below is a description of a gate that does not exist.                                                                                                                                                                                |
| CI-5 | ✅ **`make check` failed on 12 clippy errors, all introduced by the hardening work.** `cargo clippy --all-targets --all-features -- -D warnings` failed in three crates: `kata-security-reference-monitor` (5), `kata-agent-policy` (1), `kata-agent` (6). Fixed on `fix-static-checks`; the command now exits 0.                                                                                                                                                                                             | Direct evidence of what CI-0 costs. The static-check gate the project already defines has been red for the entire life of this work, and nothing reported it. One of the twelve was a real defect (CI-6), not a style nit — which is the argument for the gate rather than for suppressing it.                                                                                                                                                                                                                                     |
| CI-6 | ✅ **The FR-7 complete-mediation tests could not report what they caught.** Their failure messages used implicit format captures (`"...: {unclassified:?}"`) inside `assert!`/`panic!`, but the crate is edition 2018, where the literal is not treated as a format string — so the diagnostic printed `{unclassified:?}` verbatim instead of naming the unmediated RPC. `rpc` was flagged as an unused variable, which is the same bug seen from the other side. Fixed by passing explicit format arguments. | These are the tests that fail the build when someone adds an RPC without mediating it — the total-mediation guarantee (FR-7). They would have fired correctly but told the developer nothing about which RPC was at fault, on the exact failure they exist to make actionable.                                                                                                                                                                                                                                                     |
| CI-7 | **The default-feature build of `kata-agent` does not compile.** `cargo build` with no `--features` fails with `unresolved import crate::policy` at `rpc.rs:109`: `is_allowed_with_entrypoint` is imported unconditionally, but `mod policy` is gated behind `agent-policy`. Reproduced on `coco-parity` with this work stashed, so it predates the hardening branches. Either gate the import or make `agent-policy` a default feature.                                                                       | Found while validating that the transaction-lifecycle fixes built in both configurations. It means the only configurations anyone has built are the feature-enabled ones, and CI-1's feature matrix would fail on its first leg for a reason unrelated to the hardening work. **Re-confirmed 2026-08-02** — still reproduces at `HEAD` (`E0432`, 2 occurrences), 6 days after first being recorded. Two consequences now follow: the `cfg(not(agent-policy))` `is_allowed` stub at `rpc.rs:180-183` is a fail-open gate that can never be compiled yet still reads as live above 43 real call sites, and any test gated to that arm is silently inert — now true of `test_get_oom_event_no_deadlock` (see TEST-1). Adding `default = ["agent-policy"]` and deleting the dead arm resolves both.                                                                                                                                                                                                                                                      |
| CI-1 | **Exercise the feature matrix in CI.** `cargo check`/`test` for `--features strict-policy` and `--features agent-policy` separately.                                                                                                                                                                                                                                                                                                                                                                          | `rpc::tests::test_get_oom_event_no_deadlock` fails on `coco-parity` under `--features strict-policy` (the strict baseline policy defines no `GetOOMEventRequest` rule, so `eval_query` returns no results and the test unwraps). It fails *closed*, so it is not a security gap — but it went unnoticed, which means the strict configuration is not built or tested anywhere. That is the configuration the hardening work exists to protect. Note this leg is red today: it cannot be turned on until that test is fixed (F-22). **Update 2026-08-02:** a stopgap landed (`cce73d783`) gating that test to `not(feature = "agent-policy")`, so both legs of this matrix now `cargo check --all-targets` clean and CI-1 is unblocked. The gate is not the fix — see TEST-1. |
| CI-2 | **`cargo fmt --check` gate, crate-scoped.** Limit to `kata-agent`, `kata-security-reference-monitor`, `kata-agent-policy`, `genpolicy-fragmentgen`.                                                                                                                                                                                                                                                                                                                                                           | Confirmed: upstream kata is not fmt-clean under this repo's pinned 1.95 toolchain — `rustjail/src/cgroups{_rs.rs,/mock.rs,/notifier.rs,/systemd/manager.rs}`, `rustjail/src/mount.rs`, and four files under `runtime-rs/` all differ, and `git log` attributes each to upstream commits, not to this work. A repo-wide gate therefore cannot pass. Scoping it to the crates this work owns makes it enforceable today; those crates are currently fmt-clean.                                                                       |
| CI-3 | **Run the TLA+ model in CI** (`src/agent/security-reference-monitor/formal/run-tlc.sh`).                                                                                                                                                                                                                                                                                                                                                                                                                      | The model already encodes properties the implementation had drifted from. It is only useful if it runs.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| CI-4 | ✅ **Run the reference monitor's own test suite.** `src/agent/Makefile`'s `test` target ran `cargo test -p kata-agent`, which tests one package — so the `kata-security-reference-monitor` crate's ~116 unit tests *and* its `tests/fault_injection.rs` integration suite had never executed in CI. Fixed by adding `-p kata-security-reference-monitor`.                                                                                                                                                     | That crate holds the FR-6 transaction lifecycle. Every correctness claim the hardening makes about prepare/execute/commit/abort rested on a suite the Makefile never ran. An earlier version of this entry argued that clippy at least covered the crate, so breakage would have been caught even if behaviour was not — CI-0 and CI-5 disprove that: clippy never ran either, and the crate was in fact carrying five clippy errors. Nothing covered it.                                                                          |
| CI-8 | **No test cancels a handler future, which is the one class of defect review keeps catching by hand.** The unguarded-window defect fixed in PR #17 was an `.await` between `prepare` and `guard()` in `remove_container`: a future dropped there wedges the operation id in `Prepared` forever, and the host chooses the moment via `timeout_nano`. Nothing in any tier can see it. Crate-level SRM tests never construct a handler; `rpc::tests::srm_integration` calls helpers directly and never cancels; live pods do not cancel deterministically. Needed: an `AgentService` fixture plus a test that wraps each transactional handler in `tokio::time::timeout` and asserts the operation id is left reclaimable — no `Prepared` entry without a guard record — at every suspension point. | Cancellation safety is the whole reason `TxnGuard` exists, and it is the only FR-6 property with zero automated coverage. It was found by reading the diff, which is not a repeatable control: the same class of defect can be reintroduced by any future edit that moves a `drop(srm)` or adds an `.await`. This is also the cheapest remaining tier — it needs no cluster and no guest. |
| TEST-1 | **Restore `test_get_oom_event_no_deadlock` under `agent-policy`/`strict-policy`.** The test is currently gated to `not(feature = "agent-policy")` (`cce73d783`) because `get_oom_event` calls `is_allowed` as its first statement, and a freshly constructed `AgentPolicy` has an empty regorus engine with `allow_failures = false` — so `allow_request` returns zero results and `bail!`s, the handler errors out before reaching the `recv()` the test exercises, and the closing `unwrap()` panics. Fix: install a permissive policy on `AGENT_POLICY` before spawning the handlers. Use a shared one-time helper, not an inline call — `AgentPolicy::set_policy` is one-shot under `strict-policy`, so a second policy-driving test would otherwise fail closed. | The gate unblocks CI-1 but restores no coverage: because of CI-7 the `not(agent-policy)` arm does not compile either, so this test currently runs in **no** buildable configuration. The property it guards — the handler releases the sandbox lock before awaiting `recv()`, so concurrent `GetOOMEvent` callers cannot deadlock — lives after the policy gate and is configuration-independent, so a permissive policy makes the strict run exercise byte-identical code. It is also the only test in the crate that drives a ttrpc handler at all (1 of 44 `is_allowed` call sites), which makes it worth keeping alive rather than deleting. |
| CI-9 | **The e2e suite runs upstream's genpolicy binary, so `coco-parity`'s genpolicy work is untested end to end.** Stage 03 installs `kata-tools-static.tar.zst` and the `kata-deploy` image, both built by upstream `kata-containers/kata-containers` `main` — `ci-nightly.yaml` does not exist on this fork. The two *data* inputs are now staged from the branch (`docs/cc/e2e/03-deploy-cluster.sh`, sha256-asserted after install), which covers `rules.rego` and `genpolicy-settings.json`; the **binary** is still upstream's, and `coco-parity` changes genpolicy by 23 files / +6349 against `main`. Closing it needs either a locally built kata-tools tarball in stage 03 or a nightly on this fork. | Found by asking why stage 03 needs an artifact when stage 04 already builds — the answer is that stage 04 builds only the *guest* half. Until then, every pod policy the suite generated came from upstream rules, so genpolicy-side regressions were invisible; the FR-4A storage bijection in particular had never been exercised. This is the same missing release path F-49 describes, seen from the test side. |
| CI-11 | **No test ever exercised the fragment container-contribution contract, so the feature was unreachable in shipped code and eleven green e2e cases said nothing about it.** `rules.rego` reads a fragment's containers at `data.agent_policy.fragments[spec.feed]`, and a feed is an OCI reference, so a fragment must declare `package agent_policy.fragments["<feed>"]` — which `apply_fragment_module` rejected. Every fixture instead defined identifier-named rules in the shared `agent_policy.fragments` package, which never reaches `fragment_container_entries`. Fixed by `5476758d1` plus stage 07 cases 07l/07m, which lift a real container entry (layer root hashes included) out of a generated policy, sign it into a fragment, and assert the pod runs only when the fragment is delivered. | The pattern is worth naming separately from CI-0/CI-10: the tests were green *and* comprehensive-looking, covering declaration, delivery, SVN floors, delegation, receipts and scopes — every one of them the transport, none of them the payload. A test suite can be dense and still leave the feature's purpose untouched. The cheap control is to require, for each rule in `rules.rego`, at least one case that makes it *decide* something. |
| CI-12 | **The two fragment declaration lists were never cross-checked, so a fragment declared for delivery contributed nothing and nothing said so.** `policy_data.fragments` (BL-7, from `genpolicy-settings.json`) and `policy_fragments` (BL-8, a rule in the generated policy, and the list the *agent* reads to drive delivery) are both measured, but `fragment_container_entries` and `container_by_ref` read only the first. A fragment was fetched, verified, applied and logged as delivered while contributing no containers. Fixed by `5d9ed6000` (`all_fragment_specs`), and now covered by stage 07 case 07m. | Found within an hour of fixing F-69, on the first attempt to run the contract end to end — which is the point: the defect was one layer below the one the new test was written for, and only an executable test could reach it. The reusable control is a rule-level assertion that every measured input a policy can carry is read by the rule that consumes it; here two inputs meant the same thing and one was ignored. |
| CI-10 | **A rule that referenced an undeclared `policy_fragments` made regorus reject the whole module, so no ordinary pod could boot — and nothing noticed for a full round.** `LoadPolicyFragmentRequest if { count(policy_fragments) > 0 }` reads as fail-closed at evaluation time, but regorus treats a bare reference to a rule that is never declared as an **unsafe variable** and refuses the entire policy at compile time. genpolicy appends `policy_fragments` only for a base policy that actually declares fragments, which no ordinary workload does, so every generated policy failed to compile, `set_policy` could not build an engine, and the sandbox died before serving vsock. Fixed by `default policy_fragments := []` (`a0e8ffc30`). | Second P0 of the same shape as the `CopyFile` one, and the argument for CI-0 stated as cheaply as it can be: the only stage that exercised the rule (07) was the only thing in the tree that appended `policy_fragments`, and the fragment-free control (05) had not been re-run since the rule landed. A single `cargo test -p genpolicy` on the default path would have failed 29/29 with the exact message. |

### Feature coverage

| ID    | Item                                                                                                                                                                                                                                                                                                             | Why                                                                                                                                                                                                             |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BL-10 | **Extend SRM transaction coverage** beyond `CreateContainer`, `ExecProcess`, `SignalProcess`, and `RemoveContainer` to the remaining mutating RPCs in the complete-mediation manifest (`UpdateContainer`, `PauseContainer`/`ResumeContainer`, sandbox lifecycle, `SetIPTables`, `AddSwap`, `UpdateEphemeralMounts`, `ResizeVolume`, `CopyFile`, …). | FR-6's intent is every mutating operation. The four covered today include **every** rule that mutates persisted policy state — all `pstate` writes live inside the `CreateContainerRequest` rule, which is snapshot-wrapped — so the rollback property is complete and matches the parity anchor, which likewise wraps only a fixed set of operations. The rest are policy-gated and lifecycle-gated but not transactional. |

## Merged, but live end-to-end validation is deployment-time

These features are implemented, unit-tested, and build-clean on `coco-parity`; the remaining
validation needs a running node, an OCI registry, or a live external ledger — none of which
exist inside the confidential-guest test bed. They are **not** code gaps.

This work is now an **active workstream** (LV-1…LV-6) to produce a security-guarantees
**showcase** — each guarantee demonstrated with a GOOD (accepted) and BAD (rejected/aborted)
case. Items are sized so parallel agents can pick them up independently; dependencies are
noted. Legend: 🟢 host-feasible now · 🟠 needs a deployed strict agent / guest.

| ID   | Live validation                                                                                                                                                                                                                                                             | Feasibility                    | Depends on | Status                                                                                                                                                                                                         |
| ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------ | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| LV-0 | **Strict-agent test bed** — build the strict-policy agent + a runnable way to exercise the real SRM against live external artifacts (component-level live harnesses linking `kata-security-reference-monitor`, plus, for LV-2, a deployed guest).                           | 🟢 build now / 🟠 guest deploy | —          | ✅ done — strict rootfs (STRICT_POLICY=yes) redeployed to `/opt/kata` on `.6`; `fr1-fragment-attack.sh` 4/4                                                                                                    |
| LV-1 | **FR-4C dm-verity verified layers** — build a real EROFS/ext4 + dm-verity hash tree (`veritysetup`/`losetup`), authorize the root digest via `verified-layers.toml`, show an approved digest accepted and a tampered/other digest rejected against a real dm-verity device. | 🟢                             | LV-0       | ✅ done — `fr4c-verity-demo.sh` 5/5 (real `veritysetup` hash; SRM gate accept/reject; kernel dm-verity catches a corrupted block). Tool: `examples/verify-layer`.                                              |
| LV-2 | **FR-4D verified guest-pull images** — an allowlisted manifest digest pulls; an unlisted digest / mutable tag is denied.                                                                                                                                                    | 🟠                             | LV-0       | ✅ done — `fr4d-guestpull-demo.sh` 4/4 against a real registry manifest digest (allowlisted ok; unlisted/unpinned/empty rejected). Tool: `examples/verify-image`. In-guest CDH snapshotter is deployment-time. |
| LV-3 | **FR-1f Stage-2 SCITT/CCF receipts** — feed a real `kata-ccf-proof/v1` receipt to the agent verifier: GOOD accepted, tampered rejected.                                                                                                                                     | 🟢/🟠                          | LV-0       | ✅ done — `fr1-ccf-attack.sh` 3/3 live on a real `kata-parma` pod (valid CCF accepted; tampered/missing rejected). Tool: `mock-ledger prove-ccf`. A real Azure Confidential Ledger emits the same format.      |
| LV-4 | **BL-8/BL-9 boot OCI fragment pull + push** — fail-closed boot-pull (declared-but-unfetchable fragment → VM aborts) + `genpolicy-fragmentgen` push/consume round-trip against a real registry.                                                                              | 🟢                             | LV-0       | ⚠️ partial — reproducible as `docs/cc/e2e/07-fragment-bootpull.sh` (control + fail-closed on any cluster; GOOD-path + SVN-rollback when the feed is guest-reachable) and `docs/cc/e2e/06-policy-fragment-e2e.sh` (push/consume + OCI contract). Originally demonstrated by `bl8-bootpull-demo.sh` 2/2 and `bl9-oci-push-demo.sh` 4/4, which lived on a since-deleted VM — see F-52. **The in-guest GOOD path could never work and has been removed (F-53).** Run against a real ACR over HTTPS with anonymous pull verified from the node, 07c aborted the VM with the same `Vsock ... ENODEV` signature as the deliberately-unfetchable 07b: `load_declared_fragments()` ran in `start_sandbox()` before `rpc::start()`, and the guest gets its interfaces only from the `update_interface`/`update_routes` ttRPC handlers, so it had no network at boot-pull time. Delivery is now host-side (as in hcsshim): runtime-rs fetches each fragment named by the `io.katacontainers.config.agent.policy_fragments` annotation and pushes the COSE envelope over `LoadPolicyFragment`, while the guest records the measured declarations and refuses `create_container` until every one is satisfied. Unit-tested both sides. **Verified live 2026-08-04** on `coco-dev-2` (guest built from `9f42e055a`, dm-verity-pinned; 06 + 07 PASS, 438s): all four cases now hold — control boots, an unsatisfied declaration holds the gate, **a delivered fragment lets the pod run** (only reachable if the host fetched it, the SRM verified the COSE envelope and the module was injected), and an SVN floor above the published fragment is refused. Reaching that green required fixing F-54 (a declaration never authorized its own feed, so the SRM rejected every delivery as `UndeclaredFeed`), F-55 (a per-feed SVN floor could sink below the trust root's issuer floor) and F-57 (`LoadPolicyFragmentRequest` had no rule in `rules.rego`), plus the stale-artefact class in F-56. **Enforcement is now opt-in per declaration** (`"required": true`): the default matches C-ACI/hcsshim, where injection is lazy and an undelivered fragment simply contributes no grants, so withholding can only reduce what runs; `required: true` is stricter than C-ACI and exists for fragments whose absence is *not* fail-safe (a deny rule, an audit obligation, a constraint the base policy assumes was composed in). `required: false` is not "unchecked" — a delivered fragment is verified and cross-checked identically either way. Stage 07 gained case `07e`, identical to `07b` but for that one field and expecting the pod to boot, so the flag is proven to be read rather than the gate being unconditional or dead. **All five cases verified live 2026-08-04** on `coco-dev-2` (guest built from `e5e90cd10`, dm-verity-pinned; 03 + 07 PASS, 552s): `07a` control boots, `07b` required-and-undelivered holds the gate, `07c` delivered fragment lets the pod run, `07d` SVN floor above the published fragment refuses, `07e` optional-and-undelivered boots. **Declarations may also delegate** (`allow_nested`): a delivered fragment can declare further fragments in its own signed Rego module, but only if its authorizing declaration says so and only within the issuer scope that declaration names (`"same-issuer"`, `"any-authorized"`, or an explicit list; bare `true` is rejected because it names no scope, and the default is no delegation). This is composition, not privilege — a nested declaration still cannot introduce an issuer the measured trust root does not authorize, nor lower an SVN floor. Depth is capped at 4 as a cycle backstop, first declaration wins, and out-of-scope declarations are dropped with a logged reason rather than failing an already-committed delivery. C-ACI/hcsshim has the same capability via `candidate_fragments`, and puts the switch in the same place — `includes: ["fragments"]` on the *declaration* decides whether the fragment behind it may contribute further declarations — but that switch is a bare boolean: once set, the fragment may name any `(issuer, feed)` pair, and there is no depth cap or cycle guard. Unit-tested (6 new policy-crate tests) **and verified live 2026-08-04** on `coco-dev-2` by stage 07 cases `07f`-`07k`, which stage 06 feeds with three new signed fixtures (a child, a parent declaring it same-issuer, and a parent declaring a foreign issuer). The oracle is pod phase: a nested declaration marked `required: true` is an obligation the host never satisfies, so a registered declaration blocks `create_container` and a dropped one boots -- reading the scope decision in both directions from outside the guest. `07f` no grant -> runs (off by default); `07g` `same-issuer` -> blocked (registered); `07h` parent+child delivered -> runs (chain completes); `07i` `same-issuer` with a foreign nested issuer -> runs (dropped); `07j` explicit list naming that issuer -> blocked; `07k` `any-authorized` -> blocked. `07i` and `07j` deliver the same fragment and differ only in the grant, so neither can pass by delivery failure. **Namespace grants now come from measured state, not the fragment** (F-62): the SRM records what each `(issuer, feed)` may contribute, taken from the BL-8 declaration or the trust root, and the effective scope is that grant intersected with the fragment's own request, so neither side can widen the other. Previously the fragment's self-signed `includes` alone decided, which let any authorized issuer claim a namespace the base policy meant a different issuer to fill. A grant may also withhold the module entirely (`allow_module = false`), accepting a fragment for its SVN, receipt and ordering record while it contributes no rules — hcsshim's metadata-only load. **Receipt requirements are conjunctive** and gained hcsshim's grammar: `*` (any validated receipt), `TTL:<subject>` (validated by a key a named Trust List vouched for), or a ledger id. A list now means all-of rather than any-of, matching `fragment_receipts_ok`; single-entry lists, which is every existing configuration, are unaffected. Satisfying a conjunction needs several receipts, so `LoadPolicyFragmentRequest` carries `extra_receipts` (`<ledger>=<hex sig>`), each an additional Stage-1 countersignature over the same statement, each scoped by the same `allowed_ledgers`. **Fragments may be parameterised** (F-64): a fragment reads values via `parameter("name")` rather than hard-coding them, so one signed artefact serves several deployments without a re-sign per value. Values come from the declaration (base policy or trust root), never the fragment or the host, and are bound after signature verification by appending a constant and a total resolver (supplied → the fragment's own declared default → null) to the module. One set per declaration, where hcsshim tries every set because its loading is lazy. **A policy may declare `framework_version`** (F-65): equal or older is enforced, newer is refused. An unknown rule name in Rego is an undefined value rather than an error, so without this a policy authored for gates an older agent lacks is enforced minus those gates, silently — hcsshim denies the same case structurally. Absent is legacy and allowed; explicit-but-malformed is an error. **A container authorized by a fragment is now named by reference, not position** (F-67): policy state recorded the container's *index* into `base ++ fragment` containers, and that list is a comprehension over the fragments currently loaded — so delivering a fragment declared earlier in the list inserts entries and shifts every recorded index after it, re-binding a running container's rules to a different container at a moment the host chooses. State now stores `{feed, svn, idx}` (or `{base, idx}`), and resolution re-runs the declaration gates and pins the SVN exactly, so a container authorized under one fragment version is never evaluated against another's. **A fragment can now actually contribute a container** (F-69): the contract 
ules.rego documents keys a fragment's containers by its feed, and a feed is an OCI reference, so the only way to write that key is a quoted package path (`package agent_policy.fragments["<feed>"]`) — which the agent refused, making the whole contribution path unreachable while every delivery test stayed green. The agent now accepts exactly that form for the feed the SRM verified from the fragment's own COSE envelope, which is stricter than the shared package it sits beside: the namespace key is the signed identity, so one publisher cannot write another's. Covered live by stage 07  7l/ 7m, the C-ACI sidecar shape — a base policy pinning one workload by layer root hash and a signed fragment admitting a second container by its own layer hashes;  7l withholds the fragment and the pod must never run,  7m delivers it and both containers must become ready. |
| LV-5 | **Security-guarantees showcase** — a single scripted demo + doc presenting each guarantee (GOOD-accepted / BAD-rejected), suitable to present.                                                                                                                              | 🟢                             | LV-1…LV-4  | ✅ done — `showcase.sh` runs all proofs: **6/6 suites, 22/22 checks PASS** on node `.6`. See the private `GB200_poc` `coco/showcase.md` + `coco/harness/`.                                                     |
| LV-6 | **FR-6 transaction lifecycle under a hostile host** — drive the deployed strict agent so the transaction machinery is exercised adversarially rather than just unit-tested: short `timeout_nano` on `RemoveContainer` to force abandonment (expect reclamation, not a wedged id); a `SignalProcess` loop with fresh random `exec_id`s to confirm aborted transactions no longer accumulate; a refused `prepare` to confirm no phantom container is left in `pstate`; and a forced commit failure to observe quarantine and what the shim actually sees. | 🟠 | LV-0 | ⬜ not started — the eleven fixes in PR #17 are validated by review and unit tests only. No live tier has ever exercised FR-6. |

**Test-bed host:** GB200 node `.6` (`gb200-bm-wxjifb`), `aarch64`, Ubuntu 6.8 kernel —
docker 28.3.2, `veritysetup`/`losetup`/`cryptsetup` present, passwordless `sudo`, and
admin kubectl via `kubectl-admin`. LV-1 and LV-4 are runnable on this host today; LV-2 needs
the strict agent deployed into a Kata guest; LV-3 needs a reachable CCF/SCITT endpoint.

## Deferred / out of scope

See `docs/cc/parma-hardening-features.md` §"Deferred / out of scope" for FR-13
(snapshot/restore/migration sealing — not applicable to GPU-passthrough CC), the
hardware-gated TEE/GPU-attestation items, and the optional FR-10 content-addressed
artifact API.
